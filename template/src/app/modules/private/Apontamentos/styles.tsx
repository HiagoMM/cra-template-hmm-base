import { makeStyles } from "@material-ui/core/styles";

const useApontamentosStyles = makeStyles(theme => ({
  teste: {
    backgroundColor: theme.palette.secondary.main
  }
}));

export default useApontamentosStyles;
