import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    type: "light",
    primary: {
      main: "rgb(29, 35, 85)"
    },
    secondary: {
      main: "#FFF"
    }
  }
});

export default theme;
